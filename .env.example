# Base URL of the FastAPI pipeline's consumption API.
# Local dev: point at the running pipeline repo's uvicorn.
# Prod: api.askucits.com (the Caddy-fronted FastAPI on Hetzner).
NEXT_PUBLIC_API_BASE=http://localhost:8000
